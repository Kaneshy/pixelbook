import React from 'react'

const Footer = (color) => {
  return (
    <footer className={`p-4 mt-48 bg-[${color}] text-zinc-500 w-full flex justify-center text-center`}>
          pixelbook @
        </footer>
  )
}

export default Footer